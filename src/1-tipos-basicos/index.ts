let nome: string;
let idade: number;
let sobrenome: string;

nome = "caio";
sobrenome = "kenedy";

function nomeCompleto(nome: string, sobrenome: string) {
  return `${nome} ${sobrenome}`;
}

nomeCompleto("Caio", "Kenedy");
